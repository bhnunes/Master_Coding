Okay, let's break down this pipeline script by script and address your concerns about the mask generation process, all from a scientific rigor perspective.

Overall Pipeline Review

The pipeline you've constructed aims to create a dataset for training and evaluating a binary semantic segmentation model for prostate cancer detection in WSIs. The general flow is sound:

Image and Annotation Extraction (1 & 2): Extract patches (windows) from WSIs based on annotations, classifying them as "cancer" or "not cancer."

Color Normalization (3): Standardize the appearance of the extracted patches using Reinhard color transfer.

Dataset Splitting (4): Divide the data into training, validation, and testing sets using a stratified, grouped cross-validation approach, crucial for medical imaging.

Mask Generation (5): Create binary masks corresponding to the extracted patches.

The core steps are logically ordered. However, as you suspect, the placement and implementation of the mask generation is a significant point of concern and needs to be integrated into the earlier stages. Let's dive into each script.

Script-Specific Analysis and Recommendations

1. 1_Database_helpers.py

Purpose: Manages interaction with a MySQL database to track the processing status of WSI files. It finds SVS files, adds new cases to the database, retrieves cases to process, and updates their status after processing.

Scientific Soundness:

Database Management: Using a database is a good practice for large-scale image processing, especially when dealing with multiple processing steps and potential failures. It provides traceability and allows for resuming interrupted processes.

AddNewCases(): This function correctly identifies new SVS files and adds them to the processing queue. The use of getFolder() to prevent duplicate entries is excellent.

getCaseToProcess() and getTotalCases(): These functions efficiently manage the processing queue.

updateCase(): Records processing time, counts of cancer/non-cancer patches, and processing status. This is crucial for monitoring and analysis. The inclusion of WINDOW_SIZE, STRIDE, MATCH_PERCENTAGE, and TISSUE_PERCENTAGE in the database is excellent for reproducibility.

run_image_reader_script(): Executes the imageReader.py script as a subprocess. Crucially, it captures the output (status and comments) from the subprocess and updates the database accordingly. This error handling is excellent.

count_png_files_in_range() and countTotal(): These functions count the generated PNG files within a specific time range. This provides a way to track the output of the image extraction process.

mainProcess(): The main loop that drives the processing. It checks for new cases or processes existing ones, updating the database at each step. The logic to stop when no more cases are left is correct.

Improvements/Concerns:

Hardcoded Port: The MySQL port (3306) is hardcoded. While this is the default, it's better practice to put this in the .env file as well.

LOADCASES Variable: The use of a global boolean variable LOADCASES to switch between adding new cases and processing existing ones is functional but could be made more explicit. Perhaps a command-line argument would be clearer.

Missing error propagation: Although you check for errors in run_image_reader_script, you parse the JSON output. If result.returncode != 0 and you raise a RuntimeError, you should also update the database entry to reflect the failure. The current code doesn't explicitly do this, which would leave the database in an inconsistent state. Add a try...except block within mainProcess around the call to run_image_reader_script to catch the RuntimeError and call updateCase with a "FAILED" status.

No retry mechanism: There's no retry mechanism if processing a slide fails. Consider adding a limited number of retries before marking a case as permanently failed.

2. 2_imageReader.py

Purpose: This script takes a WSI file path, annotation file path, cancer/non-cancer color codes, and patient ID as input. It extracts image patches (windows) from the WSI based on the annotations, determines if they meet the criteria for cancer or non-cancer, and saves the qualifying patches as PNG images.

Scientific Soundness:

Annotation Parsing: The script correctly parses XML annotations, extracting coordinates for cancer and non-cancer regions. The use of shapely.geometry.Polygon is excellent for geometric calculations.

Window Generation: generate_window_coordinates efficiently generates sliding windows, considering image boundaries, window size, stride, and the bounding box of the annotated regions. The vectorized approach using NumPy is highly optimized.

Tissue Percentage Filtering: calculateTissuePercentage is a critical step. It filters out patches with insufficient tissue content, which is essential for avoiding training on background areas. The adaptive thresholding and morphological operations are standard and appropriate techniques for this task. The use of a pre-calculated kernel is a good optimization.

Intersection-over-Area (IoA) Thresholding: The script calculates the intersection area between the window and the annotated regions and checks if it meets the MATCH_PERCENTAGE. This is the correct way to determine if a window should be labeled as cancer or non-cancer. Using the area of intersection divided by the window area (rather than Intersection over Union, IoU) is appropriate here because you're classifying the entire window, not trying to precisely delineate the cancerous region within the window.

Parallel Processing: Using multiprocessing.Pool to process windows in parallel significantly speeds up the extraction process. This is highly recommended for WSI processing.

Error Handling: The try...except block within process_window handles potential errors during image reading and processing, preventing the script from crashing. Returning a failure status and error message is good practice.

Output: The script saves qualifying patches as PNG images in separate folders for cancer and non-cancer, using a naming convention that includes the patient ID, a random number, and a timestamp. This is a clear and organized way to store the extracted patches.

JSON Output: The script prints a JSON string containing the status ("COMPLETED" or "FAILED") and any comments (error messages). This is designed for communication with the calling script (1_Database_helpers.py).

Improvements/Concerns:

Openslide DLL Handling: The code correctly handles loading the OpenSlide DLL, which is crucial for working with SVS files.

Hardcoded Level: The script reads the region at level 0 (slide.read_region((x, y), 0, ...)). This assumes that level 0 is the highest resolution. While this is usually true, it's slightly more robust to explicitly check slide.level_count and use slide.level_downsamples to ensure you're getting the highest resolution if level 0 is not the highest. This is a minor point, but good practice.

No Mask Generation (Major Issue - Addressed Below): This script should be generating the masks, but it doesn't. This is the primary concern you raised.

3. 3_Reinhard color_transfer.py

Purpose: Performs Reinhard color normalization on the extracted image patches, using a single template image.

Scientific Soundness:

Reinhard Normalization: Reinhard normalization is a well-established method for color standardization in histopathology images. It helps to reduce variations in staining and illumination, making the dataset more consistent for training.

LAB Color Space: Converting images to the LAB color space is the correct approach for Reinhard normalization, as it separates luminance (L) and color (A, B) components.

Vectorized Implementation: The script uses a fully vectorized NumPy implementation, which is highly efficient. This is excellent for processing large numbers of images.

Batch Processing: The script processes images in batches, which is essential for managing memory usage when dealing with a large dataset. The use of joblib.Parallel within each batch further optimizes processing.

Improvements/Concerns:

Single Template Image: Using a single template image can be problematic. Ideally, you should use a set of template images representative of the variation in your target domain (the lab/scanner where the model will be deployed). A single image might introduce bias. Consider using multiple templates, or even better, an adaptive template selection method based on the input image's characteristics.

Hardcoded Paths: The paths to the template image, input directory, and output directory are hardcoded. Use configuration files or command-line arguments instead.

4. 4_crossfold.py

Purpose: Creates cross-validation splits for training, validation, and testing, ensuring that images from the same patient are kept within the same split (grouping).

Scientific Soundness:

Stratified Group K-Fold: This is the correct cross-validation strategy for this problem. Stratification ensures that the class distribution (cancer/non-cancer) is maintained in each fold. Grouping by patient_id is essential to prevent data leakage, as images from the same patient are highly correlated. Using StratifiedGroupKFold from scikit-learn is the standard and best practice.

Nested Cross-Validation: The inner GroupKFold split within the training/validation set is a good practice for hyperparameter tuning, although a simple train/validation split is often sufficient.

Data Augmentation: The script applies data augmentation only to the training set, which is correct. Augmenting the validation or test sets would lead to artificially inflated performance metrics.

Parallel Augmentation: Using concurrent.futures.ProcessPoolExecutor for parallel augmentation is a significant optimization, especially for CPU-bound augmentation operations. Using functools.partial to pass the output directory is a clean way to handle this.

Improvements/Concerns:

Hardcoded Paths: Again, replace hardcoded paths with configurable options.

Fixed Augmentations: The set of augmentations (rotations, flips) is fixed. Consider making this configurable, and potentially adding more augmentations relevant to histopathology (e.g., color jittering, slight blurring). Also consider using a library like Albumentations, which provides a wide range of optimized augmentation techniques.

Data Leakage Check: While theoretically correct, it is useful to add an assertion to actually check for data leakage after creating the splits:

for fold, split_data in enumerate(cross_val_splits):
    train_patients = set(split_data['train_df']['patient_id'])
    val_patients = set(split_data['val_df']['patient_id'])
    test_patients = set(split_data['test_df']['patient_id'])

    assert len(train_patients.intersection(val_patients)) == 0, f"Data leakage in fold {fold} (train/val)"
    assert len(train_patients.intersection(test_patients)) == 0, f"Data leakage in fold {fold} (train/test)"
    assert len(val_patients.intersection(test_patients)) == 0, f"Data leakage in fold {fold} (val/test)"


5. 5_MASK_generator.py

Purpose: Creates binary masks from the extracted image patches.

Scientific Soundness:

Mask Creation Logic: It assigns a value of 1 to cancer masks, 0 to not-cancer.

Improvements/Concerns:

Delayed Mask Generation (Major Issue): This script is the biggest flaw in the pipeline. Mask generation must be done at the time of patch extraction (in 2_imageReader.py), not as a separate post-processing step. Here's why:

Information Loss: By the time you reach 5_MASK_generator.py, you've lost the spatial relationship between the patches and the original WSI annotations. You're relying solely on the filename ("cancer" or "not_cancer") to determine the mask value. This is incorrect for semantic segmentation. You need to create the mask at the same time you extract the patch, using the intersection calculation already present in 2_imageReader.py.

Incorrect Masks: The current approach creates masks that are either entirely 0 or entirely 1, depending on whether the patch was classified as "cancer" or "not cancer" based on the MATCH_PERCENTAGE threshold. This is not a true semantic segmentation mask. A true mask should have 1s only for the pixels within the cancerous region within that patch and 0s elsewhere.

Redundancy: You're essentially re-iterating through all the saved images, which is inefficient.

Addressing the Mask Generation Issue: Integrating Mask Creation into 2_imageReader.py

The solution is to modify 2_imageReader.py to create the masks concurrently with patch extraction. Here's a revised process_window function and supporting modifications:

import numpy as np
import cv2
import os
from PIL import Image
from shapely.geometry import Polygon
from datetime import datetime
from multiprocessing import Pool
import random
import argparse
from dotenv import load_dotenv
import json
import warnings

# --- (Rest of your imports) ---

load_dotenv(override=True)

# Constants from environment variables (same as before)
WINDOW_SIZE = int(os.getenv('WINDOW_SIZE'))
STRIDE = int(os.getenv('STRIDE'))
MATCH_PERCENTAGE = float(os.getenv('MATCH_PERCENTAGE'))  # Keep this, but it's not directly used for mask creation anymore
TISSUE_PERCENTAGE = float(os.getenv('TISSUE_PERCENTAGE'))
OPENSLIDE_PATH = os.getenv('OPENSLIDE_PATH')

if hasattr(os, 'add_dll_directory'):
    with os.add_dll_directory(OPENSLIDE_PATH):
        from openslide import OpenSlide
else:
    from openslide import OpenSlide

# Pre-calculate kernel (same as before)
KERNEL = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (5, 5))

# --- (updateMax function - same as before) ---

def updateMax(Xmax, Xmin, Ymax, Ymin, x, y):
    Xmax = max(Xmax, x)
    Xmin = min(Xmin, x)
    Ymax = max(Ymax, y)
    Ymin = min(Ymin, y)
    return Xmax, Xmin, Ymax, Ymin

# --- (calculateTissuePercentage function - same as before) ---

def calculateTissuePercentage(window_np):
    img_bgr = cv2.cvtColor(window_np, cv2.COLOR_RGB2BGR)
    gray = cv2.cvtColor(img_bgr, cv2.COLOR_BGR2GRAY)
    thresh = cv2.adaptiveThreshold(gray, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C, 
                                   cv2.THRESH_BINARY_INV, 11, 2)
    cleaned = cv2.morphologyEx(thresh, cv2.MORPH_CLOSE, KERNEL)
    contours, _ = cv2.findContours(cleaned, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    tissue_area = sum(cv2.contourArea(contour) for contour in contours)
    tile_area = window_np.shape[0] * window_np.shape[1]
    return (tissue_area / tile_area) >= TISSUE_PERCENTAGE

# --- (generate_window_coordinates function - same as before) ---
def generate_window_coordinates(image_width, image_height, window_size, stride, Xmax, Xmin, Ymax, Ymin):
    x_indices = np.arange(0, image_width - window_size + 1, stride)
    y_indices = np.arange(0, image_height - window_size + 1, stride)

    valid_x = (x_indices + window_size > Xmin) & (x_indices < Xmax)
    valid_y = (y_indices + window_size > Ymin) & (y_indices < Ymax)

    valid_x_indices = x_indices[valid_x]
    valid_y_indices = y_indices[valid_y]

    windows = np.array(np.meshgrid(valid_x_indices, valid_y_indices)).T.reshape(-1, 2)
    windows = np.hstack((windows, windows + window_size))

    return windows.tolist()

def create_mask_for_window(window_x, window_y, window_size, annotations_cancer):
    """Creates a binary mask for a given window based on cancer annotations."""
    mask = np.zeros((window_size, window_size), dtype=np.uint8)
    window_polygon = Polygon([(window_x, window_y), (window_x + window_size, window_y),
                             (window_x + window_size, window_y + window_size), (window_x, window_y)])

    for annotation in annotations_cancer:
        annotation_polygon = Polygon(annotation)
        if window_polygon.intersects(annotation_polygon):
            # Create a polygon for the intersection
            intersection = window_polygon.intersection(annotation_polygon)

            # Convert intersection polygon to pixel coordinates relative to the window
            if intersection.is_empty:
                continue  # Skip if the intersection is empty

            if intersection.type == 'MultiPolygon':
                for p in intersection.geoms:
                    coords = np.array(p.exterior.coords)
                    # Convert global coordinates to local window coordinates
                    coords[:, 0] -= window_x
                    coords[:, 1] -= window_y
                    # Ensure coordinates are within the window bounds and are integers
                    coords = np.round(np.clip(coords, 0, window_size - 1)).astype(np.int32)
                    cv2.fillPoly(mask, [coords], 1)  # Fill with 1 for cancer
            elif intersection.type == 'Polygon':
                coords = np.array(intersection.exterior.coords)
                # Convert global coordinates to local window coordinates
                coords[:, 0] -= window_x
                coords[:, 1] -= window_y
                # Ensure coordinates are within the window bounds and are integers
                coords = np.round(np.clip(coords, 0, window_size - 1)).astype(np.int32)
                cv2.fillPoly(mask, [coords], 1)
            # else:  # Handle other geometry types if needed (e.g., LineString, Point)
               # print('Intersection is not a polygon:', intersection.type)
    return mask

def process_window(args):
    path_Image, path_cancer_folder, path_not_cancer_folder, path_mask_folder, patient, x, y, window_size, annotations_cancer, annotations_not_cancer, MATCH_PERCENTAGE = args

    try:
        slide = OpenSlide(path_Image)
        window = slide.read_region((x, y), 0, (window_size, window_size)).convert("RGB")
        window_np = np.array(window)
        slide.close()

        # --- Mask Creation ---
        mask = create_mask_for_window(x, y, window_size, annotations_cancer)
        is_cancer_patch = np.sum(mask) > 0 # Check if *any* part of the window is cancer.


        if calculateTissuePercentage(window_np):
            patch_image = Image.fromarray(window_np)
            now = datetime.now()
            timestamp = now.strftime('%m_%d_%Y_%H_%M_%S')
            random_number = random.randint(1, 10000)

            if is_cancer_patch:
                file_path = f"CANCER_PATIENT_{patient}_{random_number}_{timestamp}.png"
                patch_image.save(os.path.join(path_cancer_folder, file_path))
                mask_image = Image.fromarray(mask * 255)  # Save as grayscale image (0 and 255)
                mask_image.save(os.path.join(path_mask_folder, file_path)) #same name of the image file
            else:
                #It only saves if there is tissue AND if does not pass the cancer test
                file_path = f"NOT_CANCER_PATIENT_{patient}_{random_number}_{timestamp}.png"
                patch_image.save(os.path.join(path_not_cancer_folder, file_path))
                # We still save a mask of all zeros for non-cancer patches:
                mask_image = Image.fromarray(mask * 255)  # This will be all zeros.
                mask_image.save(os.path.join(path_mask_folder, file_path))#same name of the image file


            return True, None

        return True, None  # Tissue percentage not met
    except Exception as e:
        return False, str(e)

def createWindows(path_Image, cancer_color, not_cancer_color):
    # --- (Same as before, but now returns annotations_cancer) ---
    Xmax, Xmin, Ymax, Ymin = 0, float('inf'), 0, float('inf')

    path_Annotation = path_Image.replace('.svs', '.xml', 1)
    slide = OpenSlide(path_Image)
    image_width, image_height = slide.dimensions
    slide.close()

    annotations_tree = ET.parse(path_Annotation)
    annotations_root = annotations_tree.getroot()

    annotations_cancer = []
    annotations_not_cancer = []

    for annotation in annotations_root.findall('Annotation'):
        if annotation.get('LineColor') == str(cancer_color):
            layer_region = annotation.findall("Regions//Region")
            for region in layer_region:
                temp = []
                vertices = region.findall("Vertices//Vertex")
                for vertex in vertices:
                    x = float(vertex.get("X"))
                    y = float(vertex.get("Y"))
                    Xmax, Xmin, Ymax, Ymin = updateMax(Xmax, Xmin, Ymax, Ymin, x, y)
                    temp.append((x, y))
                annotations_cancer.append(temp)
        elif annotation.get('LineColor') == str(not_cancer_color):
            layer_region = annotation.findall("Regions//Region")
            for region in layer_region:
                temp = []
                vertices = region.findall("Vertices//Vertex")
                for vertex in vertices:
                    x = float(vertex.get("X"))
                    y = float(vertex.get("Y"))
                    Xmax, Xmin, Ymax, Ymin = updateMax(Xmax, Xmin, Ymax, Ymin, x, y)
                    temp.append((x, y))
                annotations_not_cancer.append(temp)

    windows = generate_window_coordinates(image_width, image_height, WINDOW_SIZE, STRIDE, Xmax, Xmin, Ymax, Ymin)
    return windows, annotations_not_cancer, annotations_cancer

if __name__ == '__main__':
    warnings.filterwarnings("ignore")
    try:
        parser = argparse.ArgumentParser(description='Process images')
        parser.add_argument('--path_Image', type=str, help='Path to the image file')
        parser.add_argument('--path_cancer_folder', type=str, help='Path to the cancer folder')
        parser.add_argument('--path_not_cancer_folder', type=str, help='Path to the not cancer folder')
        parser.add_argument('--path_mask_folder', type=str, help='Path to the mask folder')  # NEW
        parser.add_argument('--cancer_color', type=str, help='Color code for cancer')
        parser.add_argument('--not_cancer_color', type=str, help='Color code for not cancer')
        parser.add_argument('--patient', type=str, help='Patient ID')
        args = parser.parse_args()

        path_Image = args.path_Image
        path_cancer_folder = args.path_cancer_folder
        path_not_cancer_folder = args.path_not_cancer_folder
        path_mask_folder = args.path_mask_folder  # NEW
        cancer_color = args.cancer_color
        not_cancer_color = args.not_cancer_color
        patient = args.patient

        # Ensure the mask folder exists
        os.makedirs(path_mask_folder, exist_ok=True)  # NEW

        windows, annotations_not_cancer, annotations_cancer = createWindows(path_Image, cancer_color, not_cancer_color)

        # Prepare arguments for parallel processing, including path_mask_folder
        args_list = [(path_Image, path_cancer_folder, path_not_cancer_folder, path_mask_folder, patient, x, y,
                 WINDOW_SIZE, annotations_cancer, annotations_not_cancer, MATCH_PERCENTAGE) for x, y, _, _ in
                windows]
      
        with Pool(processes=os.cpu_count()) as pool:
            results = pool.map(process_window, args_list)

        for success, message in results:
            if not success:
                raise Exception(message)

        status = 'COMPLETED'
        comments = ''
    except Exception as e:
        status = 'FAILED'
        comments = str(e)
    finally:
        data = {
            "status": status,
            "comments": comments
        }
        json_data = json.dumps(data)
        print(json_data)
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END

Key Changes and Explanations:

create_mask_for_window(window_x, window_y, window_size, annotations_cancer): This new function takes the window coordinates, size, and the list of cancer annotations. It creates a binary mask of the same size as the window. It iterates through the cancer annotations and uses cv2.fillPoly to fill the corresponding regions in the mask with 1. The key here is converting the global WSI coordinates of the annotations to local window coordinates (coords[:, 0] -= window_x, coords[:, 1] -= window_y). The use of np.clip is important to make sure coordinates are within the image.

process_window(...) Changes:

It now takes path_mask_folder as an argument.

It calls create_mask_for_window to generate the mask before any other processing.

The is_cancer_patch variable now determines if any part of the created mask contains cancer.

Saves both the image patch and its corresponding mask to the appropriate folders. The filenames for the image and mask are identical, ensuring easy pairing. Critically, both cancer and non-cancer patches get masks. Non-cancer patches will have masks that are all zeros.

if __name__ == '__main__': Changes:

Added --path_mask_folder as a command-line argument.

os.makedirs(path_mask_folder, exist_ok=True): Ensures the mask directory exists.

The argument list passed to pool.map now includes path_mask_folder.

Changes in 1_Database_helpers.py

You need to slightly modify 1_Database_helpers.py to accommodate the new path_mask_folder argument.

# ... (rest of your imports and functions) ...

def run_image_reader_script(path_Image, path_cancer_folder, path_not_cancer_folder, path_mask_folder, cancer_color, not_cancer_color, patient): #added the mask folder
    command = [
        PYTHON_PATH,
        IMAGE_READER_PATH,
        '--path_Image', path_Image,
        '--path_cancer_folder', path_cancer_folder,
        '--path_not_cancer_folder', path_not_cancer_folder,
        '--path_mask_folder', path_mask_folder,  # Add the new argument
        '--cancer_color', cancer_color,
        '--not_cancer_color', not_cancer_color,
        '--patient', patient
    ]

    # --- (rest of the function remains the same) ---
    result = subprocess.run(command, capture_output=True, text=True)
    
    # Check if the script ran successfully
    if result.returncode!= 0:
        raise RuntimeError(f"Error running imageReader_refactored.py: {result.stderr}")
    
    output_json = result.stdout
    parsed_output = json.loads(output_json)
    status = parsed_output.get("status")
    comments = parsed_output.get("comments")
    
    return status, comments

def mainProcess():
    if LOADCASES==True:
        AddNewCases()
    else:
        stop=False
        totalCases=len(getTotalCases())
        while stop==False:
            data=getCaseToProcess()
            if len(data)==0:
                stop=True
            else:
                id_cur=data[0]['ID']
                path_Image_current=data[0]['FILEPATH']
                path_cancer_folder_current=PATH_CANCER_FOLDER
                path_not_cancer_folder_current=PATH_NOT_CANCER_FOLDER
                path_mask_folder_current = PATH_MASK_FOLDER # Added mask folder
                cancer_color_current=data[0]['CANCER_COLOR']
                not_cancer_color_current=data[0]['NOT_CANCER_COLOR']
                patient_current=data[0]['PATIENT']
                start_time = time.time()
                try: #added try...except block to catch the RuntimeError and call updateCase with a "FAILED"
                    status, comments = run_image_reader_script(
                    path_Image=path_Image_current,
                    path_cancer_folder=path_cancer_folder_current,
                    path_not_cancer_folder=path_not_cancer_folder_current,
                    path_mask_folder=path_mask_folder_current, #added mask folder
                    cancer_color=cancer_color_current,
                    not_cancer_color=not_cancer_color_current,
                    patient=patient_current)
                except RuntimeError as e:
                    status = 'FAILED'
                    comments = str(e)
                    updateCase(id_cur, 0, 0, 0, status, comments, WINDOW_SIZE, STRIDE, TISSUE_PERCENTAGE, MATCH_PERCENTAGE)
                    continue  # Go to the next case
                end_time = time.time()
                execution_time = (end_time - start_time)/60
                count_cancer=countTotal(start_time, end_time,PATH_CANCER_FOLDER )
                count_not_cancer=countTotal(start_time, end_time,PATH_NOT_CANCER_FOLDER)
                updateCase(id_cur, count_cancer, count_not_cancer, execution_time, status, comments,WINDOW_SIZE,STRIDE,TISSUE_PERCENTAGE,MATCH_PERCENTAGE)
                totalCases=totalCases-1
                print("There are "+str(totalCases)+" images to be processed yet")


if __name__ == '__main__':
    load_dotenv(override=True)
    global IMAGESPATH
    global PATH_CANCER_FOLDER
    global PATH_NOT_CANCER_FOLDER
    global PATH_MASK_FOLDER #created the mask folder
    global WINDOW_SIZE
    global STRIDE
    global MATCH_PERCENTAGE
    global TISSUE_PERCENTAGE
    global LOADCASES
    global IMAGE_READER_PATH
    global PYTHON_PATH
    IMAGESPATH=os.getenv('IMAGESPATH')
    PATH_CANCER_FOLDER=os.getenv('PATH_CANCER_FOLDER')
    PATH_NOT_CANCER_FOLDER=os.getenv('PATH_NOT_CANCER_FOLDER')
    PATH_MASK_FOLDER = os.getenv('PATH_MASK_FOLDER') #created mask folder
    WINDOW_SIZE=os.getenv('WINDOW_SIZE')
    STRIDE=os.getenv('STRIDE')
    MATCH_PERCENTAGE=str(os.getenv('MATCH_PERCENTAGE'))
    TISSUE_PERCENTAGE=str(os.getenv('TISSUE_PERCENTAGE'))
    IMAGE_READER_PATH=str(os.getenv('IMAGE_READER_PATH'))
    PYTHON_PATH=str(os.getenv('PYTHON_PATH'))
    LOADCASES=False # True to add cases
    # Add PATH_MASK_FOLDER to your .env file:
    # PATH_MASK_FOLDER=D://Usuario//Desktop//Base_de_dados//MASTER//MASK
    mainProcess()
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END

Eliminate 5_MASK_generator.py

You can now completely remove 5_MASK_generator.py from your pipeline, as its functionality is fully integrated into 2_imageReader.py.

Updated Pipeline Summary (Scientifically Sound):

1_Database_helpers.py: Manages the processing queue, finds new WSI files, and updates the database with processing status, counts, and parameters. Improved error handling and database consistency.

2_imageReader.py: Extracts patches and generates corresponding binary masks based on annotations. Uses tissue percentage filtering and handles errors. Now correctly generates masks.

3_Reinhard color_transfer.py: Performs color normalization. Consider using multiple template images.

4_crossfold.py: Creates stratified, grouped cross-validation splits. Added data leakage check.

This revised pipeline is now scientifically sound for training a binary semantic segmentation model. The critical change is the integration of mask generation into the patch extraction process, ensuring the correct spatial relationship between images and masks. The use of a database, appropriate cross-validation, and color normalization all contribute to a robust and reproducible workflow. Remember to update your .env file with the PATH_MASK_FOLDER variable.