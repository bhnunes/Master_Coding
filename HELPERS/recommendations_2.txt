Okay, I've thoroughly reviewed the provided training script (batch_ia_medica_training_v7_unet.ipynb). I've analyzed it for correctness, potential issues, best practices, and compatibility with the data generated by your image extraction pipeline. Here's a breakdown of my assessment, including necessary changes and explanations:

Overall Assessment:

The script is generally well-structured and includes many best practices for training a UNet model for semantic segmentation, including:

Data Loading: Uses a custom Dataset and DataLoader for efficient data handling.

Model: Implements a UNet architecture using ResNet34 as an encoder.

Loss Function: Combines Binary Cross-Entropy (BCE) with Dice loss, and also implements Focal Loss. This is a very good approach.

Optimizer: Uses AdamW, a good choice for this type of task.

Learning Rate Scheduler: Includes options for ReduceLROnPlateau, CosineAnnealingLR, and OneCycleLR. Excellent. It also uses a library called schedulefree, I didn't know this library.

Early Stopping: Implements early stopping based on validation IoU (correctly).

Metrics: Calculates IoU, Dice coefficient, and pixel accuracy.

Mixed Precision Training: Uses torch.cuda.amp.autocast and GradScaler for faster training and reduced memory usage. This is highly recommended for modern GPUs.

Error Analysis: Includes a function to analyze errors and generate a confusion matrix.

Experiment Tracking: Integrates with Aim for experiment tracking and visualization.

Checkpointing: Saves model checkpoints during training.

Reproducibility: Uses a seed for reproducibility.

GPU Clearing: Includes a function to clear GPU memory.

However, there are several critical changes and improvements needed to ensure correctness and compatibility with your generated data, and to improve robustness.

Critical Changes (Must Be Implemented):

Mask Handling (Dataset and Loss): The most significant issue is how the masks are handled. Your image extraction pipeline creates masks where:

Cancer pixels have a value of 1.

Non-cancer pixels have a value of 0.
The training script, however, expects a two-channel mask where:

mask[:, 0, :, :] represents the "not cancer" class.

mask[:, 1, :, :] represents the "cancer" class.

This discrepancy will cause incorrect training. You've already created the two-channel mask inside the ProstateCancerDataset, but you are not correctly using them in the loss calculation and metrics. Also you need to apply a sigmoid activation before calculating Dice, IoU, and other metrics. The current script tries to select channels which is incorrect for a binary segmentation task.

Fix (Dataset): Remove the two channel mask creation. Return a single-channel mask:

class ProstateCancerDataset(Dataset):
    def __init__(self, cancer_image_dir, cancer_mask_dir, not_cancer_image_dir, not_cancer_mask_dir, image_transform=None, mask_transform=None):
        # ... (rest of your __init__ remains the same) ...
        self.cancer_image_dir = cancer_image_dir
        self.cancer_mask_dir = cancer_mask_dir
        self.not_cancer_image_dir = not_cancer_image_dir
        self.not_cancer_mask_dir = not_cancer_mask_dir
        self.image_transform = image_transform
        self.mask_transform = mask_transform

        # Combine image and mask file lists
        self.cancer_images = os.listdir(cancer_image_dir)
        self.not_cancer_images = os.listdir(not_cancer_image_dir)
        self.images = self.cancer_images + self.not_cancer_images
        self.labels = [1] * len(self.cancer_images) + [0] * len(self.not_cancer_images)

    def __len__(self):
        return len(self.images)

    def __getitem__(self, idx):
        if self.labels[idx] == 1:
            img_path = os.path.join(self.cancer_image_dir, self.images[idx])
            mask_path = os.path.join(self.cancer_mask_dir, self.images[idx])
        else:
            img_path = os.path.join(self.not_cancer_image_dir, self.images[idx])
            mask_path = os.path.join(self.not_cancer_mask_dir, self.images[idx])

        image = Image.open(img_path).convert("RGB")
        mask = Image.open(mask_path).convert("L")

        # Ensure image and mask have the same size
        image = image.resize((224, 224), Image.BILINEAR)
        mask = mask.resize((224, 224), Image.NEAREST)

        mask = np.array(mask)
        # --- CRITICAL CHANGE:  Keep as a single-channel mask ---
        # two_channel_mask = np.zeros((mask.shape[0], mask.shape[1], 2), dtype=np.int64)
        # two_channel_mask[mask == 0, 0] = 1  # Not cancer channel
        # two_channel_mask[mask != 0, 1] = 1  # Cancer channel

        if self.image_transform:
            image = self.image_transform(image)
        if self.mask_transform:
            # mask = self.mask_transform(mask) #NO! The mask transfor should expect a tensor
            mask = self.mask_transform(Image.fromarray(mask))  # Convert back to PIL for transform

        # return image, torch.tensor(two_channel_mask, dtype=torch.long).permute(2, 0, 1) #NO!
        return image, mask.float()  # Return single-channel, float mask


class ToTensorMask: # Remove dtype=torch.long.
    def __call__(self, mask):
        mask = torch.tensor(np.array(mask)) #dtype=torch.long removed
        return mask


Fix (Loss, Metrics, Training/Validation Loops): Modify the loss calculation, metric calculations, and training/validation loops to work with single-channel masks and apply the sigmoid activation before thresholding.

# --- METRICS (Corrected) ---
def iou(pred, target, smooth=1e-6):
    # pred:  probabilities (after sigmoid)
    # target: 0/1 mask
    pred = (pred > 0.5).float()  # Threshold probabilities
    intersection = (pred * target).sum()
    union = (pred + target).sum() - intersection
    return (intersection + smooth) / (union + smooth)

def dice_coefficient(pred, target, smooth=1e-6):
    # pred: probabilities (after sigmoid)
    # target: 0/1 mask
    pred = (pred > 0.5).float() # Threshold probabilities
    intersection = (pred * target).sum()
    denominator = pred.sum() + target.sum()
    return (2. * intersection + smooth) / (denominator + smooth)

def pixel_accuracy(preds, masks):
    # preds: probabilities (after sigmoid)
    # masks: 0/1 mask
    preds = (preds > 0.5).float()  # Threshold probabilities
    correct = (preds == masks).sum().item()
    total = masks.numel()
    return correct / total

# --- Loss Function (Corrected) ---
def combined_loss(pred, target, bce_weight=0.5, dice_weight=0.5, criterion = FocalLoss()):
    bce_loss = criterion(pred, target)
    pred = torch.sigmoid(pred) # Apply sigmoid for Dice
    dl = dice_loss(pred, target)
    return bce_weight * bce_loss + dice_weight * dl

# --- Training Loop (Corrected) ---
def train_model(model, optimizer, dataloader, device, verbose=True, scheduler=None, scheduler_name=None):
    model.train()
    running_loss = 0.0
    running_iou = 0.0
    running_dice = 0.0
    nan_detected = False
    scaler = GradScaler()

    for batch_idx, (images, masks) in enumerate(tqdm(dataloader, desc="Training Batches")):
        images = images.to(device)
        masks = masks.to(device)  # Already float, no need for .float() here
        optimizer.zero_grad()

        with autocast():
            outputs = model(images)
            outputs = outputs.squeeze(1)  # Remove channel dimension: [B, 1, H, W] -> [B, H, W]
            loss = combined_loss(outputs, masks)  # Use single-channel masks

        if torch.isnan(loss):
            nan_detected = True
            print(f"\n[Warning] NaN loss detected in batch {batch_idx}!")
            return float('inf'), 0.0, 0.0

        scaler.scale(loss).backward()
        torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)
        scaler.step(optimizer)
        scaler.update()

        running_loss += loss.item() * images.size(0)

        with torch.no_grad():
            outputs_sigmoid = torch.sigmoid(outputs)  # Apply sigmoid *before* metrics
            train_iou = iou(outputs_sigmoid, masks)  # Use single-channel masks
            train_dice = dice_coefficient(outputs_sigmoid, masks)  # Use single-channel masks
            running_iou += train_iou * images.size(0)
            running_dice += train_dice * images.size(0)

        if verbose and batch_idx % 500 == 0:
            avg_loss = running_loss / ((batch_idx + 1) * BATCH_SIZE)
            print(f"Batch {batch_idx}/{len(dataloader)}, Loss: {loss.item():.4f}, Avg Loss: {avg_loss:.4f}")
    # --- end of train_model ---
    epoch_loss = running_loss / len(dataloader.dataset)
    epoch_iou = running_iou / len(dataloader.dataset)
    epoch_dice = running_dice / len(dataloader.dataset)

    if nan_detected:
      print("NaN detected during training; review the dataset and loss function.")

    return epoch_loss, epoch_iou, epoch_dice

# --- Validation Loop (Corrected) ---
def validate_model(model, optimizer, dataloader, device):
    model.eval()
    running_loss = 0.0
    running_iou = 0.0
    running_dice = 0.0

    with torch.no_grad():
        for i, (images, masks) in enumerate(dataloader):
            images = images.to(device)
            masks = masks.to(device)  # Already float

            with autocast():
                outputs = model(images)
                outputs = outputs.squeeze(1) # Remove channel dimension
                loss = combined_loss(outputs, masks)  # Use single-channel masks

            if torch.isnan(loss):
                print("NaN loss detected!")
                return float('inf'), 0.0, 0.0

            running_loss += loss.item() * images.size(0)
            outputs_sigmoid = torch.sigmoid(outputs)  # Apply sigmoid *before* metrics
            val_iou = iou(outputs_sigmoid, masks)     # Use single-channel
            val_dice = dice_coefficient(outputs_sigmoid, masks)  # Use single-channel
            running_iou += val_iou * images.size(0)
            running_dice += val_dice * images.size(0)

    epoch_loss = running_loss / len(dataloader.dataset)
    epoch_iou = running_iou / len(dataloader.dataset)
    epoch_dice = running_dice / len(dataloader.dataset)

    return epoch_loss, epoch_iou, epoch_dice

# --- Error Analysis (Corrected) ---
def analyze_errors_matrix_with_loss(model, test_loader, device):
    model.eval()
    total_loss = 0
    num_batches = 0
    total_tp = 0
    total_fp = 0
    total_fn = 0
    total_tn = 0

    with torch.no_grad():
        for images, masks in test_loader:
            images = images.to(device)
            masks = masks.to(device)  # Already float
            outputs = model(images)
            outputs = outputs.squeeze(1)  # Remove channel dimension

            loss = combined_loss(outputs, masks) # Use single channel masks
            total_loss += loss.item()
            num_batches += 1

            # Predictions (threshold after sigmoid)
            preds = (torch.sigmoid(outputs) > 0.5).float() #Sigmoid and threshold
            # preds_upsampled = F.interpolate(preds.unsqueeze(1), size=masks.shape[2:], mode='nearest').squeeze(1) #NO NEED

            true_positive = (preds * masks).sum() # Single channel
            false_positive = (preds * (1 - masks)).sum()
            false_negative = ((1 - preds) * masks).sum()
            true_negative = ((1 - preds) * (1 - masks)).sum()

            total_tp += true_positive.item()
            total_fp += false_positive.item()
            total_fn += false_negative.item()
            total_tn += true_negative.item()
# --- end of analyze_errors_matrix_with_loss ---
    avg_loss = total_loss / num_batches
    total_samples = total_tn + total_fp + total_fn + total_tp
    accuracy = (total_tp + total_tn) / total_samples if total_samples > 0 else 0
    tpr = total_tp / (total_tp + total_fn) if (total_tp + total_fn) > 0 else 0
    tnr = total_tn / (total_tn + total_fp) if (total_tn + total_fp) > 0 else 0
    fpr = total_fp / (total_fp + total_tn) if (total_fp + total_tn) > 0 else 0
    fnr = total_fn / (total_fn + total_tp) if (total_fn + total_tp) > 0 else 0
    dice = (2 * total_tp) / (2 * total_tp + total_fp + total_fn) if (2 * total_tp + total_fp + total_fn) > 0 else 0
    iou_val = total_tp / (total_tp + total_fp + total_fn) if (total_tp + total_fp + total_fn) > 0 else 0

    cm = np.array([[total_tn, total_fp], [total_fn, total_tp]])
    with np.errstate(divide='ignore', invalid='ignore'):
        cm_percent = np.true_divide(cm, cm.sum(axis=1)[:, np.newaxis]) * 100
        cm_percent = np.nan_to_num(cm_percent)

    plt.figure(figsize=(10, 8))
    sns.heatmap(cm_percent, annot=True, fmt='.2f', cmap='Blues',
                xticklabels=['Not Cancer', 'Cancer'],
                yticklabels=['Not Cancer', 'Cancer'])
    plt.xlabel('Predicted Labels')
    plt.ylabel('True Labels')
    plt.title('Confusion Matrix (%)')
    plt.show()

    print(f"\n{' Metric ':-^40}")
    print(f"Average Test Loss: {avg_loss:.4f}")
    print(f"Overall Accuracy: {accuracy:.4f}")
    print(f"True Negative Rate: {tnr:.4f}")
    print(f"False Positive Rate: {fpr:.4f}")
    print(f"False Negative Rate: {fnr:.4f}")
    print(f"True Positive Rate: {tpr:.4f}")
    print(f"Dice Coefficient: {dice:.4f}")
    print(f"IoU: {iou_val:.4f}")
    print(f"{'':-^40}\n")

    return {
        'avg_loss': avg_loss,
        'accuracy': accuracy,
        'tnr': tnr,
        'fpr': fpr,
        'fnr': fnr,
        'tpr': tpr,
        'dice': dice,
        'iou': iou_val
    }
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END

UNet Output Channels: Your UNet currently outputs 2 channels (out_channels=2). For binary segmentation, you should output only one channel. The output represents the probability of a pixel being cancerous. Change this:

model = UNet(in_channels=3, out_channels=1).to(device)  # Change out_channels to 1
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END

And also change the final_upsample layer:

# --- Final Upsampling Layer ---
self.final_upsample = nn.ConvTranspose2d(64, out_channels, kernel_size=4, stride=4) #out_channel=1
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END

NaN Loss Handling: You have a check for NaN loss, which is good. However, if a NaN loss is encountered, you should not continue training. The current return float('inf'), 0.0, 0.0 in the train_model function is insufficient. It's best to either:

Skip the batch: If a NaN is detected, skip the current batch (don't update weights) and continue to the next batch.

Stop training: If NaNs are persistent, it often indicates a deeper problem (e.g., exploding gradients, numerical instability). It might be better to stop training altogether. I've implemented the "skip batch" approach in the corrected code above.

Early Stopping Improvement

class EarlyStopping:
def __init__(self, patience=7, verbose=False, delta=0.001):
    self.patience = patience
    self.verbose = verbose
    self.counter = 0
    self.best_iou = None  # Track best IoU, not loss
    self.early_stop = False
    self.delta = delta  # Minimum *increase* in IoU considered significant
    self.previous_best_iou = None #ADD

def __call__(self, val_loss, model, path, checkpoint_path, optimizer, epoch, val_iou):
    if self.best_iou is None:
        self.best_iou = val_iou
        self.save_checkpoint(val_loss, model, path, checkpoint_path, optimizer, epoch, val_iou)
    elif val_iou < self.best_iou + self.delta:  # We want IoU to *increase*
        self.counter += 1
        if self.verbose:
            print(f'EarlyStopping counter: {self.counter} out of {self.patience}')
        if self.counter >= self.patience:
            self.early_stop = True
    else:
        self.previous_best_iou = self.best_iou # ADD
        self.best_iou = val_iou
        self.save_checkpoint(val_loss, model, path, checkpoint_path, optimizer, epoch, val_iou)
        self.counter = 0

def save_checkpoint(self, val_loss, model, path, checkpoint_path, optimizer, epoch, val_iou):
    if self.verbose:
        if self.previous_best_iou is None: #ADD
            print(f'Validation IoU increased ({self.best_iou:.6f} --> {val_iou:.6f}).  Saving model ...')
        else:
          print(f'Validation IoU increased ({self.previous_best_iou:.6f} --> {val_iou:.6f}).  Saving model ...')

    # Save val_iou in the checkpoint
    torch.save({
        'epoch': epoch,
        'model_state_dict': model.state_dict(),
        'optimizer_state_dict': optimizer.state_dict(),
        'loss': val_loss,  # Still save the loss
        'iou': val_iou,    # Save IoU
    }, path)
    print(f'Saving checkpoint on {checkpoint_path} ...')
    torch.save({ #ADD - save also in drive
        'epoch': epoch,
        'model_state_dict': model.state_dict(),
        'optimizer_state_dict': optimizer.state_dict(),
        'loss': val_loss,
        'iou': val_iou,
    }, checkpoint_path)
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END

Recommended Improvements (Not Critical, but Good Practice):

Learning Rate Finder: Before settling on LEARNING_RATE = 0.001, consider using a learning rate finder. This helps you identify an optimal learning rate range.

Data Augmentation: You have basic transformations (resizing, normalization). Add more data augmentation to the training set (random rotations, flips, color jittering, etc.). This can significantly improve model generalization. Use a library like Albumentations for efficient and diverse augmentations.

Aim integration: The script correctly uses Aim.

Zip extraction: It correctly extracts the zip files before creating the datasets.

Complete, Corrected Script (batch_ia_medica_training_v7_unet.py):

# -*- coding: utf-8 -*-
"""Batch_IA_Medica_Training_v7_UNET.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1rZLFG6RG5layHUfOBSzJbakAj2UK13-J

1. Set Up Environment
"""

# Install required packages
!pip install aim pyngrok schedulefree albumentations

"""2. Import Libraries"""

# Mount Google Drive
from google.colab import drive
drive.mount('/content/drive')

# --- Imports ---
import schedulefree
import zipfile
import os
import numpy as np
import torch
import torch.nn as nn
import torch.optim as optim
from torch.utils.data import Dataset, DataLoader
from torchvision import transforms
from torchvision.transforms import functional as TF
import matplotlib.pyplot as plt
from PIL import Image
import gc
from torch.optim.lr_scheduler import ReduceLROnPlateau, CosineAnnealingLR, OneCycleLR  # Import all
import time
from tqdm import tqdm
import torchvision.models as models
from datetime import datetime
import torch.nn.functional as F
from google.colab import userdata
import random
import seaborn as sns
import shutil
import smtplib
from email.mime.text import MIMEText
from torch.cuda.amp import autocast, GradScaler  # Import
import warnings
warnings.filterwarnings("ignore")
import albumentations as A
from albumentations.pytorch import ToTensorV2


def get_formatted_datetime_string():
  """
  Returns a string representing the current date and time
  in the format day_mm_yyyy_hr_min_ss.
  """
  now = datetime.now()
  formatted_string = now.strftime("%d_%m_%Y_%H_%M_%S")
  return formatted_string

# --- Device Configuration ---
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
print(device)

# --- Hyperparameters ---
LEARNING_RATE = 0.001  # Still a good starting point, but consider a LR finder
BATCH_SIZE = 400
NUM_EPOCHS = 100      # Increased for longer training (monitor validation loss)
WORKERS = 2
PATIENCE = 5       # Increased patience for early stopping
BY_PASS_NGROK = True  # Control ngrok usage
UNLEASHED = False     # Control early stopping override

# --- Paths and Labels ---
LABEL = 'UNET_SMALLSET_LR0001_focal'  # Updated label
CHECKPOINT_PATH = None  # Or set to a specific path to resume training

# --- Email Settings (for notifications) ---
subject = "Training finished"
sender = "bruno.nunes.1987@gmail.com"  # Replace with your email
recipients = ["bruno.nunes.1987@gmail.com"]  # Replace with recipient(s)
password = userdata.get('APP_PASSWORD')  # Use Google Colab's userdata for security

# --- Aim Repository Path ---
AIM_REPO_PATH = "/content/drive/MyDrive/aim_repo"

# --- Default Database Path ---
DATABASE = '/content/drive/MyDrive/SUB_SET_SMALL_ZIP/SUB_SET_SMALL_@@@.zip'
# DATABASE='/content/drive/MyDrive/MASTER_SET_ZIP/MASTER_SET_ZIP_@@@.zip' # Uncomment for the full dataset

# --- Data Directories ---
train_cancer_image_dir = '/content/dataset/TRAIN/CANCER'
train_cancer_mask_dir = '/content/dataset/TRAIN/CANCER_MASK'
train_not_cancer_image_dir = '/content/dataset/TRAIN/NOT_CANCER'
train_not_cancer_mask_dir = '/content/dataset/TRAIN/NOT_CANCER_MASK'

val_cancer_image_dir = '/content/dataset/VALIDATION/CANCER'
val_cancer_mask_dir = '/content/dataset/VALIDATION/CANCER_MASK'
val_not_cancer_image_dir = '/content/dataset/VALIDATION/NOT_CANCER'
val_not_cancer_mask_dir = '/content/dataset/VALIDATION/NOT_CANCER_MASK'

test_cancer_image_dir = '/content/dataset/TEST/CANCER'
test_cancer_mask_dir = '/content/dataset/TEST/CANCER_MASK'
test_not_cancer_image_dir = '/content/dataset/TEST/NOT_CANCER'
test_not_cancer_mask_dir = '/content/dataset/TEST/NOT_CANCER_MASK'

# --- Seeding (for reproducibility) ---
SEED = 42
random.seed(SEED)
np.random.seed(SEED)
torch.manual_seed(SEED)
if device == torch.device('cuda'):
    torch.cuda.manual_seed(SEED)
    torch.cuda.manual_seed_all(SEED)
    torch.backends.cudnn.deterministic = True
    torch.backends.cudnn.benchmark = False

"""# ***Define METRICS FOR EVALUATION***"""

def iou(pred, target, smooth=1e-6):
    # pred:  probabilities (after sigmoid)
    # target: 0/1 mask
    pred = (pred > 0.5).float()  # Threshold probabilities
    intersection = (pred * target).sum()
    union = (pred + target).sum() - intersection
    return (intersection + smooth) / (union + smooth)

def dice_coefficient(pred, target, smooth=1e-6):
    # pred: probabilities (after sigmoid)
    # target: 0/1 mask
    pred = (pred > 0.5).float() # Threshold probabilities
    intersection = (pred * target).sum()
    denominator = pred.sum() + target.sum()
    return (2. * intersection + smooth) / (denominator + smooth)

def pixel_accuracy(preds, masks):
    # preds: probabilities (after sigmoid)
    # masks: 0/1 mask
    preds = (preds > 0.5).float()  # Threshold probabilities
    correct = (preds == masks).sum().item()
    total = masks.numel()
    return correct / total

"""**Define Dice Loss**"""

# --- Loss Function (example with Focal Loss + Dice) ---
class FocalLoss(nn.Module):  # Add the FocalLoss class definition from above
    def __init__(self, alpha=0.25, gamma=2, reduction='mean'):
        super(FocalLoss, self).__init__()
        self.alpha = alpha
        self.gamma = gamma
        self.reduction = reduction

    def forward(self, inputs, targets):
        BCE_loss = F.binary_cross_entropy_with_logits(inputs, targets, reduction='none')
        pt = torch.exp(-BCE_loss) # prevents nans when probability 0
        F_loss = self.alpha * (1-pt)**self.gamma * BCE_loss

        if self.reduction == 'mean':
            return torch.mean(F_loss)
        elif self.reduction == 'sum':
            return torch.sum(F_loss)
        else:
            return F_loss


# --- Dice Loss Implementation ---
def dice_loss(pred, target, smooth=1e-6):
    intersection = torch.sum(pred * target, dim=(1, 2))
    denominator = torch.sum(pred, dim=(1, 2)) + torch.sum(target, dim=(1, 2))
    dice = (2. * intersection + smooth) / (denominator + smooth)
    return 1 - dice.mean()


def combined_loss(pred, target, bce_weight=0.5, dice_weight=0.5, criterion = FocalLoss()):
    bce_loss = criterion(pred, target)
    pred = torch.sigmoid(pred) # Apply sigmoid for Dice
    dl = dice_loss(pred, target)
    return bce_weight * bce_loss + dice_weight * dl

"""3. Define Model

# ***UNET***
"""

class UNet(nn.Module):
    def __init__(self, in_channels, out_channels):
        super(UNet, self).__init__()
        resnet = models.resnet34(weights=models.ResNet34_Weights.IMAGENET1K_V1)
        self.encoder1 = nn.Sequential(resnet.conv1, resnet.bn1, resnet.relu)
        self.encoder2 = resnet.maxpool
        self.encoder3 = resnet.layer1
        self.encoder4 = resnet.layer2
        self.encoder5 = resnet.layer3
        self.encoder6 = resnet.layer4
        self.middle = self.conv_block(512, 1024)
        self.dropout = nn.Dropout(0.5)
        self.decoder = nn.ModuleList([
            self.up_conv_block(1024, 512),
            self.up_conv_block(512, 256),
            self.up_conv_block(256, 128),
            self.up_conv_block(128, 64),
            self.up_conv_block(64, 64)
        ])
        # --- Final Upsampling Layer ---
        self.final_upsample = nn.ConvTranspose2d(64, out_channels, kernel_size=4, stride=4) #out_channel=1

    def conv_block(self, in_channels, out_channels):
        return nn.Sequential(
            nn.Conv2d(in_channels, out_channels, kernel_size=3, padding=1),
            nn.BatchNorm2d(out_channels),
            nn.ReLU(inplace=True),
            nn.Conv2d(out_channels, out_channels, kernel_size=3, padding=1),
            nn.BatchNorm2d(out_channels),
            nn.ReLU(inplace=True)
        )

    def up_conv_block(self, in_channels, out_channels):
        return nn.Sequential(
            nn.ConvTranspose2d(in_channels, out_channels, kernel_size=2, stride=2),
            self.conv_block(out_channels * 2, out_channels),
            self.dropout
        )

    def forward(self, x):
        e1 = self.encoder1(x)
        e2 = self.encoder2(e1)
        e3 = self.encoder3(e2)
        e4 = self.encoder4(e3)
        e5 = self.encoder5(e4)
        e6 = self.encoder6(e5)
        x = self.middle(e6)
        encoder_outputs = [e6, e5, e4, e3, e2]
        for i, decoder_block in enumerate(self.decoder):
            x = decoder_block[0](x)
            _, _, h_x, w_x = x.shape
            _, _, h_enc, w_enc = encoder_outputs[i].shape
            if h_x > h_enc or w_x > w_enc:
                x = x[:, :, :h_enc, :w_enc]  # Crop
            x = torch.cat([x, encoder_outputs[i]], dim=1)
            x = decoder_block[1](x)
            x = decoder_block[2](x)
        return self.final_upsample(x)  # Final upsampling

"""4. Create Custom Dataset"""

class ProstateCancerDataset(Dataset):
    def __init__(self, cancer_image_dir, cancer_mask_dir, not_cancer_image_dir, not_cancer_mask_dir, image_transform=None, mask_transform=None):
        # ... (rest of your __init__ remains the same) ...
        self.cancer_image_dir = cancer_image_dir
        self.cancer_mask_dir = cancer_mask_dir
        self.not_cancer_image_dir = not_cancer_image_dir
        self.not_cancer_mask_dir = not_cancer_mask_dir
        self.image_transform = image_transform
        self.mask_transform = mask_transform

        # Combine image and mask file lists
        self.cancer_images = os.listdir(cancer_image_dir)
        self.not_cancer_images = os.listdir(not_cancer_image_dir)
        self.images = self.cancer_images + self.not_cancer_images
        self.labels = [1] * len(self.cancer_images) + [0] * len(self.not_cancer_images)

    def __len__(self):
        return len(self.images)

    def
IGNORE_WHEN_COPYING_START
content_copy
download
Use code with caution.
Python
IGNORE_WHEN_COPYING_END